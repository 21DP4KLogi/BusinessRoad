{
  "dependencies": {
    "esbuild": "^0.24.2",
    "sprae": "^11.0.4"
  },
  "scripts": {
    "dev": "esbuild script.ts style.css --watch --bundle --outdir=../dist --sourcemap",
    "preview": "esbuild script.ts style.css --watch --bundle --outdir=../dist --minify"
  }
}
